# Core
anthropic>=0.30.0
python-dotenv>=1.0.0

# Graph database
neo4j>=5.19.0

# UI
streamlit>=1.35.0
pyvis>=0.3.2

# Paper fetching
requests>=2.31.0
arxiv>=2.1.0

# Email
# Uses stdlib smtplib — no extra package needed

# Embeddings / vector store (for Q&A agent later)
numpy>=1.26.0
# openai>=1.0.0  # uncomment if using OpenAI embeddings

# Dev / testing
pytest>=8.0.0
